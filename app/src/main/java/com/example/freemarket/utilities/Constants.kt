package com.example.freemarket.utilities

interface Constants {
    companion object {
        const val TAG = "Free Market"
        const val DATE_FORMAT = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'"
        const val DATE_TIME_FORMAT = "yyyy-MMM-dd HH:mm:ss"
        const val DATE_FORMAT_SHORT = "yyyy/MM/dd"
        const val DIALOG = "dialog"
        const val TYPE = "type"
        const val TITLE = "title"
        const val MSG = "msg"
    }
}